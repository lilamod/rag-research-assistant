# URL of the deployed FastAPI backend (no trailing slash).
# Leave this file absent / unset for local dev — Vite's proxy handles it.
# Set it as a real Environment Variable in the Vercel project settings
# (not by uploading this file) for the production/preview deployments.
VITE_API_URL=https://your-backend-host.example.com
